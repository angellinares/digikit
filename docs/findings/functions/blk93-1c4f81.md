# blk93@0x1c4f81 — internal conditional continuation

- **Bounds**: inventory entry `0x1c4f81`, split `0x1c4f81`–`0x1c5334`; this
  inventory entry is an internal continuation, not an independently callable
  function. **[V]**
- **Entry/exit**: a conditional jump from `blk93@0x1c4ecf` at `0x1c4f25`
  enters here; the bounded scan identifies it as the sole direct internal
  branch, and it is conditional. `0x1c5264` jumps back to that entry's shared
  restore/return epilogue at `0x1c4f56`–`0x1c4f7c`. **[V][D]**
- **Delayed calls**: `0x1c5193` and `0x1c51f8` call `0x1c06ba`; `0x1c525d`
  calls `0xb80000`. **[V]**

## What it does

The continuation has carry-linked add/sub instructions at `0x1c50f5`,
`0x1c50f8`, `0x1c5304`, `0x1c5306`, `0x1c531c`, and `0x1c531f`. It has
literal 64-iteration loops at `0x1c5096`–`0x1c5101` and
`0x1c52ab`–`0x1c5328`. **[V]**

It materializes `0x25d940` exactly twice in this path: into `I5` at
`0x1c50aa` in the first literal-64 loop and into `I1` at `0x1c52bf` in the
second. `DM(I1,M5)=R2` at `0x1c52de` is an indexed store in that second loop.
The concrete same-base read/access shapes are also verified. **[V]** No
initializer was found in the covered decoded blocks, but this bounded coverage
is not a whole-firmware absence result: aliases, register-built addresses,
omitted or uncertain code,
and runtime initialization remain unexamined. Table, coefficient, working-bank,
per-machine-storage, and ownership interpretations remain **[O]**.

The call path and structure support a resampler interpretation, but its exact
algorithm remains **[D]**. Six-tap, polyphase, and windowed-sinc descriptions
remain **[O]**. The inherited `I4` context reads include offsets `+98` through
`+109`, but the context identity and the broad semantic interpretation of
those fields remain **[D]**; SRC-page ownership is **[O]**.

## Relationship to the callable entry

[`blk93@0x1c4ecf`](blk93-1c4ecf.md) is the callable entry. Within the stated
aligned/depth>=8 decoded-block coverage, one known direct call is the
unconditional `0x1c6b00 -> 0x1c4ecf` call from `blk93@0x1c642a`; this bounded
coverage does not claim global exclusivity. This file documents only its
conditionally taken continuation. **[V]** It does not establish a machine
selector, a machine-specific bank, or universal execution. **[D][O]**

## Modification seam (provisional)

For observation or patching of this bank, the evidenced lowest-risk point is
writer `0x1c52de`. Altering either loop body or the table globally may affect
all users. Adding a machine-specific dispatch is high risk until the runtime
function-pointer mapping at `0x254d98` is known. These are experiments, not
established ownership or a patch plan. **[D][O]**

## Evidence

Deterministic dossier:
`out/sharc-engine/phase-b/blk93-1c4f81.txt` (DT2 1.16
`section_7_BLOB.bin`, SHA-256
`0f514a12a2255f5c081e292c47f1f29462003177658da4bbae0a22fd737fffa2`),
matching the verified source blob. **[V]**

Reproducible bounded extraction: `out/sharc-engine/machine-path/summary.txt`
(SHA-256 `fb7a6a080015e4ce173f745dde17160aa5970931b85ad2a2919a3a22cb5301e4`);
the two identical extraction records hash to
`73c5ce0ad83f7bb0e92156841f27b06aa7a74fe808d2f29848756ba1bf98b218`.
**[D]**
